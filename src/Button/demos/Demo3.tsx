import { ImportButton } from 'ant-design-ext';
import React from 'react';

const Demo: React.FC = () => {
  return (
    <>
      <ImportButton>导入</ImportButton>
    </>
  );
};

export default React.memo(Demo);
